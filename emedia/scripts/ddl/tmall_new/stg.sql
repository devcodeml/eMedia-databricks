DROP TABLE IF EXISTS `stg`.`emedia_sem_audience_mapping`;
CREATE TABLE `stg`.`emedia_sem_audience_mapping` (
  `audience_name` STRING,
  `definition` STRING,
  `audience_keyword` STRING,
  `platform` STRING,
  `id` STRING,
  `cate_code` STRING)
USING delta
LOCATION  'dbfs:/mnt/prod/data_warehouse/media_stg.db/emedia_sem_audience_mapping';


DROP TABLE IF EXISTS `stg`.`ylmf_adzone_daily`;
CREATE TABLE `stg`.`ylmf_adzone_daily` (
  `ad_date` STRING,
  `campaign_group_id` STRING,
  `campaign_group_name` STRING,
  `campaign_id` STRING,
  `campaign_name` STRING,
  `promotion_entity_id` STRING,
  `promotion_entity_name` STRING,
  `adzone_id` STRING,
  `adzone_name` STRING,
  `add_new_charge` STRING,
  `add_new_uv` STRING,
  `alipay_inshop_amt` STRING,
  `alipay_inshop_num` STRING,
  `cart_num` STRING,
  `charge` STRING,
  `click` STRING,
  `cpm` STRING,
  `ctr` STRING,
  `deep_inshop_pv` STRING,
  `dir_shop_col_num` STRING,
  `gmv_inshop_amt` STRING,
  `gmv_inshop_num` STRING,
  `icvr` STRING,
  `impression` STRING,
  `inshop_item_col_num` STRING,
  `inshop_potential_uv` STRING,
  `inshop_pv` STRING,
  `inshop_pv_rate` STRING,
  `inshop_uv` STRING,
  `prepay_inshop_amt` STRING,
  `prepay_inshop_num` STRING,
  `return_pv` STRING,
  `search_click_cnt` STRING,
  `biz_code` STRING,
  `offset` STRING,
  `campaign_id_list` STRING,
  `page_size` STRING,
  `query_time_dim` STRING,
  `query_domain` STRING,
  `group_by_campaign_id` STRING,
  `group_by_log_date` STRING,
  `group_by_promotion_entity_id` STRING,
  `start_time` STRING,
  `end_time` STRING,
  `effect_type` STRING,
  `effect` STRING,
  `effect_days` STRING,
  `req_storeId` STRING,
  `dw_resource` STRING,
  `dw_create_time` STRING,
  `dw_batch_number` STRING,
  `etl_date` DATE,
  `etl_create_time` TIMESTAMP)
USING delta
LOCATION  'dbfs:/mnt/prod/data_warehouse/media_stg.db/ylmf_adzone_daily';

DROP TABLE IF EXISTS `stg`.`feedflow_adzone_daily`;
CREATE TABLE `stg`.`feedflow_adzone_daily` (
  `store_id` STRING,
  `ad_date` TIMESTAMP,
  `category_id` STRING,
  `brand_id` STRING,
  `campaign_id` STRING,
  `campaign_name` STRING,
  `adgroup_id` STRING,
  `adgroup_name` STRING,
  `adzone_id` STRING,
  `adzone_name` STRING,
  `effect` STRING,
  `charge` DECIMAL(20,4),
  `ad_pv` DECIMAL(20,4),
  `clicks` DECIMAL(20,4),
  `ecpm` DECIMAL(20,4),
  `ecpc` DECIMAL(20,4),
  `inshop_pv` DECIMAL(20,4),
  `inshop_uv` DECIMAL(20,4),
  `inshop_uv_rate` DECIMAL(20,4),
  `deep_inshop_num` DECIMAL(20,4),
  `avg_access_time` DECIMAL(20,4),
  `avg_access_page_num` DECIMAL(20,4),
  `follow_number` DECIMAL(20,4),
  `add_new_uv` DECIMAL(20,4),
  `add_new_uv_rate` DECIMAL(20,4),
  `new_f_charge` DECIMAL(20,4),
  `inshop_item_col_num` DECIMAL(20,4),
  `cart_num` DECIMAL(20,4),
  `gmv_inshop_num` DECIMAL(20,4),
  `gmv_inshop_amt` DECIMAL(20,4),
  `alipay_in_shop_num` DECIMAL(20,4),
  `alipay_inshop_amt` DECIMAL(20,4),
  `icvr` DECIMAL(20,4),
  `cvr` DECIMAL(20,4),
  `roi` DECIMAL(20,4),
  `data_source` STRING,
  `dw_etl_date` STRING,
  `dw_batch_id` STRING,
  `dmp` STRING)
USING delta
LOCATION  'dbfs:/mnt/prod/data_warehouse/media_stg.db/feedflow_adzone_daily';

DROP TABLE IF EXISTS `stg`.`super_zz_adzone_daily`;
CREATE TABLE `stg`.`super_zz_adzone_daily` (
  `ad_pv` DECIMAL(20,4),
  `alipay_in_shop_num` DECIMAL(20,4),
  `alipay_inshop_amt` DECIMAL(20,4),
  `alipay_uv` DECIMAL(20,4),
  `alipay_uv_cost` DECIMAL(20,4),
  `avg_access_page_num` DECIMAL(20,4),
  `avg_access_time` DECIMAL(20,4),
  `campaign_group_id` STRING,
  `campaign_group_name` STRING,
  `campaign_id` STRING,
  `campaign_name` STRING,
  `cart_num` DECIMAL(20,4),
  `cost` DECIMAL(20,4),
  `click` DECIMAL(20,4),
  `ctr` DECIMAL(20,4),
  `cvr` DECIMAL(20,4),
  `deep_inshop_uv` DECIMAL(20,4),
  `deep_interest` DECIMAL(20,4),
  `deep_interest_cost` DECIMAL(20,4),
  `dir_shop_col_num` STRING,
  `dw_batch_id` STRING,
  `dw_etl_date` STRING,
  `data_source` STRING,
  `ecpc` DECIMAL(20,4),
  `ecpm` DECIMAL(20,4),
  `gmv_inshop_amt` DECIMAL(20,4),
  `gmv_inshop_num` DECIMAL(20,4),
  `inshop_item_col_num` DECIMAL(20,4),
  `inshop_uv` DECIMAL(20,4),
  `inshop_uv_cost` DECIMAL(20,4),
  `item_shop_cat_cost` DECIMAL(20,4),
  `pv_uv` DECIMAL(20,4),
  `biz_code` STRING,
  `effect` STRING,
  `ad_date` TIMESTAMP,
  `roi` DECIMAL(20,4),
  `shop_id` STRING,
  `category_id` STRING,
  `brand_id` STRING,
  `adzone_id` STRING,
  `adzone_name` STRING,
  `effect_days` STRING)
USING delta
LOCATION  'dbfs:/mnt/prod/data_warehouse/media_stg.db/super_zz_adzone_daily';

DROP TABLE IF EXISTS `stg`.`ylmf_campaign_daily`;
CREATE TABLE `stg`.`ylmf_campaign_daily` (
  `ad_date` STRING,
  `campaign_group_id` STRING,
  `campaign_group_name` STRING,
  `campaign_id` STRING,
  `campaign_name` STRING,
  `add_new_charge` STRING,
  `add_new_uv` STRING,
  `add_new_uv_cost` STRING,
  `add_new_uv_rate` STRING,
  `alipay_inshop_amt` STRING,
  `alipay_inshop_num` STRING,
  `avg_access_page_num` STRING,
  `avg_deep_access_times` STRING,
  `cart_num` STRING,
  `charge` STRING,
  `click` STRING,
  `cpc` STRING,
  `cpm` STRING,
  `ctr` STRING,
  `cvr` STRING,
  `deep_inshop_pv` STRING,
  `dir_shop_col_num` STRING,
  `gmv_inshop_amt` STRING,
  `gmv_inshop_num` STRING,
  `icvr` STRING,
  `impression` STRING,
  `inshop_item_col_num` STRING,
  `inshop_potential_uv` STRING,
  `inshop_potential_uv_rate` STRING,
  `inshop_pv` STRING,
  `inshop_pv_rate` STRING,
  `inshop_uv` STRING,
  `prepay_inshop_amt` STRING,
  `prepay_inshop_num` STRING,
  `return_pv` STRING,
  `return_pv_cost` STRING,
  `roi` STRING,
  `search_click_cnt` STRING,
  `search_click_cost` STRING,
  `biz_code` STRING,
  `offset` STRING,
  `page_size` STRING,
  `query_time_dim` STRING,
  `query_domain` STRING,
  `start_time` STRING,
  `end_time` STRING,
  `effect_type` STRING,
  `effect` STRING,
  `effect_days` STRING,
  `req_storeId` STRING,
  `dw_resource` STRING,
  `dw_create_time` STRING,
  `dw_batch_number` STRING,
  `etl_date` DATE,
  `etl_create_time` TIMESTAMP)
USING delta
LOCATION  'dbfs:/mnt/prod/data_warehouse/media_stg.db/ylmf_campaign_daily';


DROP TABLE IF EXISTS `stg`.`ylmf_campaign_group_daily`;
CREATE TABLE `stg`.`ylmf_campaign_group_daily` (
  `ad_date` STRING,
  `campaign_group_id` STRING,
  `campaign_group_name` STRING,
  `add_new_charge` STRING,
  `add_new_uv` STRING,
  `add_new_uv_cost` STRING,
  `add_new_uv_rate` STRING,
  `alipay_inshop_amt` STRING,
  `alipay_inshop_num` STRING,
  `avg_access_page_num` STRING,
  `avg_deep_access_times` STRING,
  `cart_num` STRING,
  `charge` STRING,
  `click` STRING,
  `cpc` STRING,
  `cpm` STRING,
  `ctr` STRING,
  `cvr` STRING,
  `deep_inshop_pv` STRING,
  `dir_shop_col_num` STRING,
  `gmv_inshop_amt` STRING,
  `gmv_inshop_num` STRING,
  `icvr` STRING,
  `impression` STRING,
  `inshop_item_col_num` STRING,
  `inshop_potential_uv` STRING,
  `inshop_potential_uv_rate` STRING,
  `inshop_pv` STRING,
  `inshop_pv_rate` STRING,
  `inshop_uv` STRING,
  `prepay_inshop_amt` STRING,
  `prepay_inshop_num` STRING,
  `return_pv` STRING,
  `return_pv_cost` STRING,
  `roi` STRING,
  `search_click_cnt` STRING,
  `search_click_cost` STRING,
  `biz_code` STRING,
  `offset` STRING,
  `page_size` STRING,
  `query_time_dim` STRING,
  `query_domain` STRING,
  `start_time` STRING,
  `end_time` STRING,
  `effect_type` STRING,
  `effect` STRING,
  `effect_days` STRING,
  `req_storeId` STRING,
  `dw_resource` STRING,
  `dw_create_time` STRING,
  `dw_batch_number` STRING,
  `etl_date` DATE,
  `etl_create_time` TIMESTAMP)
USING delta
LOCATION  'dbfs:/mnt/prod/data_warehouse/media_stg.db/ylmf_campaign_group_daily';


DROP TABLE IF EXISTS `stg`.`super_zz_campaign_daily`;
CREATE TABLE `stg`.`super_zz_campaign_daily` (
  `ad_pv` DECIMAL(20,4),
  `alipay_in_shop_num` DECIMAL(20,4),
  `alipay_inshop_amt` DECIMAL(20,4),
  `alipay_uv` DECIMAL(20,4),
  `alipay_uv_cost` DECIMAL(20,4),
  `avg_access_page_num` DECIMAL(20,4),
  `avg_access_time` DECIMAL(20,4),
  `campaign_group_id` STRING,
  `campaign_group_name` STRING,
  `campaign_id` STRING,
  `campaign_name` STRING,
  `cart_num` DECIMAL(20,4),
  `cost` DECIMAL(20,4),
  `click` DECIMAL(20,4),
  `ctr` DECIMAL(20,4),
  `cvr` DECIMAL(20,4),
  `deep_inshop_uv` DECIMAL(20,4),
  `deep_interest` DECIMAL(20,4),
  `deep_interest_cost` DECIMAL(20,4),
  `dir_shop_col_num` STRING,
  `dw_batch_id` STRING,
  `dw_etl_date` STRING,
  `data_source` STRING,
  `ecpc` DECIMAL(20,4),
  `ecpm` DECIMAL(20,4),
  `gmv_inshop_amt` DECIMAL(20,4),
  `gmv_inshop_num` DECIMAL(20,4),
  `inshop_item_col_num` DECIMAL(20,4),
  `inshop_uv` STRING,
  `inshop_uv_cost` STRING,
  `item_shop_cat_cost` STRING,
  `pv_uv` DECIMAL(20,4),
  `biz_code` STRING,
  `effect` STRING,
  `ad_date` TIMESTAMP,
  `roi` DECIMAL(20,4),
  `shop_id` STRING,
  `category_id` STRING,
  `brand_id` STRING,
  `effect_days` STRING,
  `type` STRING)
USING delta
LOCATION  'dbfs:/mnt/prod/data_warehouse/media_stg.db/super_zz_campaign_daily';


DROP TABLE IF EXISTS `stg`.`ylmf_creative_package_daily`;
CREATE TABLE `stg`.`ylmf_creative_package_daily` (
  `ad_date` STRING,
  `campaign_group_id` STRING,
  `campaign_group_name` STRING,
  `campaign_id` STRING,
  `campaign_name` STRING,
  `creative_package_id` BIGINT,
  `creative_package_name` STRING,
  `promotion_entity_id` STRING,
  `promotion_entity_name` STRING,
  `add_new_charge` STRING,
  `add_new_uv` STRING,
  `add_new_uv_cost` STRING,
  `add_new_uv_rate` STRING,
  `alipay_inshop_amt` STRING,
  `alipay_inshop_num` STRING,
  `avg_access_page_num` STRING,
  `avg_deep_access_times` STRING,
  `cart_num` STRING,
  `charge` STRING,
  `click` STRING,
  `cpc` STRING,
  `cpm` STRING,
  `ctr` STRING,
  `cvr` STRING,
  `deep_inshop_pv` STRING,
  `dir_shop_col_num` STRING,
  `gmv_inshop_amt` STRING,
  `gmv_inshop_num` STRING,
  `icvr` STRING,
  `impression` STRING,
  `inshop_item_col_num` STRING,
  `inshop_potential_uv` STRING,
  `inshop_potential_uv_rate` STRING,
  `inshop_pv` STRING,
  `inshop_pv_rate` STRING,
  `inshop_uv` STRING,
  `prepay_inshop_amt` STRING,
  `prepay_inshop_num` STRING,
  `return_pv` STRING,
  `return_pv_cost` STRING,
  `roi` STRING,
  `search_click_cnt` STRING,
  `search_click_cost` STRING,
  `biz_code` STRING,
  `offset` STRING,
  `page_size` STRING,
  `query_time_dim` STRING,
  `query_domain` STRING,
  `group_by_campaign_id` STRING,
  `group_by_log_date` STRING,
  `group_by_promotion_entity_id` STRING,
  `start_time` STRING,
  `end_time` STRING,
  `effect_type` STRING,
  `effect` STRING,
  `effect_days` STRING,
  `req_storeId` STRING,
  `dw_resource` STRING,
  `dw_create_time` STRING,
  `dw_batch_number` STRING,
  `etl_date` DATE,
  `etl_create_time` TIMESTAMP)
USING delta
LOCATION  'dbfs:/mnt/prod/data_warehouse/media_stg.db/ylmf_creative_package_daily';


DROP TABLE IF EXISTS `stg`.`super_zz_creative_daily`;
CREATE TABLE `stg`.`super_zz_creative_daily` (
  `ad_pv` DECIMAL(20,4),
  `alipay_in_shop_num` DECIMAL(20,4),
  `alipay_inshop_amt` DECIMAL(20,4),
  `alipay_uv` DECIMAL(20,4),
  `alipay_uv_cost` DECIMAL(20,4),
  `avg_access_page_num` DECIMAL(20,4),
  `avg_access_time` DECIMAL(20,4),
  `campaign_group_id` STRING,
  `campaign_group_name` STRING,
  `campaign_id` STRING,
  `campaign_name` STRING,
  `cart_num` DECIMAL(20,4),
  `cost` DECIMAL(20,4),
  `click` DECIMAL(20,4),
  `creative_id` STRING,
  `creative_name` STRING,
  `ctr` DECIMAL(20,4),
  `cvr` DECIMAL(20,4),
  `deep_inshop_uv` DECIMAL(20,4),
  `deep_interest` DECIMAL(20,4),
  `deep_interest_cost` DECIMAL(20,4),
  `dir_shop_col_num` STRING,
  `dw_batch_id` STRING,
  `dw_etl_date` STRING,
  `data_source` STRING,
  `ecpc` DECIMAL(20,4),
  `ecpm` DECIMAL(20,4),
  `gmv_inshop_amt` DECIMAL(20,4),
  `gmv_inshop_num` DECIMAL(20,4),
  `inshop_item_col_num` DECIMAL(20,4),
  `inshop_uv` DECIMAL(20,4),
  `inshop_uv_cost` DECIMAL(20,4),
  `item_shop_cat_cost` DECIMAL(20,4),
  `pv_uv` DECIMAL(20,4),
  `biz_code` STRING,
  `effect` STRING,
  `ad_date` TIMESTAMP,
  `roi` DECIMAL(20,4),
  `shop_id` STRING,
  `category_id` STRING,
  `brand_id` STRING,
  `effect_days` STRING)
USING delta
LOCATION  'dbfs:/mnt/prod/data_warehouse/media_stg.db/super_zz_creative_daily';


DROP TABLE IF EXISTS `stg`.`feedflow_creative_daily`;
CREATE TABLE `stg`.`feedflow_creative_daily` (
  `store_id` STRING,
  `ad_date` TIMESTAMP,
  `category_id` STRING,
  `brand_id` STRING,
  `campaign_id` STRING,
  `campaign_name` STRING,
  `adgroup_id` STRING,
  `adgroup_name` STRING,
  `creative_id` STRING,
  `creative_name` STRING,
  `effect` STRING,
  `charge` DECIMAL(20,4),
  `ad_pv` DECIMAL(20,4),
  `clicks` DECIMAL(20,4),
  `ecpm` STRING,
  `ecpc` STRING,
  `inshop_pv` STRING,
  `inshop_uv` STRING,
  `inshop_uv_rate` STRING,
  `deep_inshop_num` STRING,
  `avg_access_time` STRING,
  `avg_access_page_num` STRING,
  `follow_number` STRING,
  `add_new_uv` STRING,
  `add_new_uv_rate` STRING,
  `new_f_charge` STRING,
  `inshop_item_col_num` STRING,
  `cart_num` DECIMAL(20,4),
  `gmv_inshop_num` STRING,
  `gmv_inshop_amt` STRING,
  `alipay_in_shop_num` DECIMAL(20,4),
  `alipay_inshop_amt` DECIMAL(20,4),
  `icvr` STRING,
  `cvr` STRING,
  `roi` STRING,
  `data_source` STRING,
  `dw_etl_date` STRING,
  `dw_batch_id` STRING,
  `dmp` STRING)
USING delta
LOCATION  'dbfs:/mnt/prod/data_warehouse/media_stg.db/feedflow_creative_daily';


DROP TABLE IF EXISTS `stg`.`zz_creative_daily`;
CREATE TABLE `stg`.`zz_creative_daily` (
  `ad_date` STRING,
  `campaign_id` STRING,
  `campaign_name` STRING,
  `adgroup_id` STRING,
  `adgroup_name` STRING,
  `category_id` STRING,
  `brand_id` STRING,
  `creative_id` STRING,
  `creative_name` STRING,
  `image_url` STRING,
  `click_url` STRING,
  `store_id` STRING,
  `sku_id` STRING,
  `cost` DECIMAL(20,4),
  `clicks` DECIMAL(20,0),
  `impressions` DECIMAL(20,0),
  `uv` DECIMAL(20,0),
  `total_cart_quantity` DECIMAL(20,4),
  `order_quantity` DECIMAL(20,4),
  `order_value` DECIMAL(20,4),
  `favorite_item_quantity` DECIMAL(20,4),
  `favorite_shop_quantity` DECIMAL(20,4),
  `avg_access_page_quantity` DECIMAL(20,4),
  `avg_access_time_length` DECIMAL(20,4),
  `gmv_value` DECIMAL(20,4),
  `gmv_quantity` DECIMAL(20,4),
  `deep_inshop_uv` DECIMAL(20,4),
  `effect` STRING,
  `effect_days` STRING,
  `req_campaign_model` STRING,
  `req_effect_type` STRING,
  `dw_batch_id` STRING,
  `type` STRING)
USING delta
LOCATION  'dbfs:/mnt/prod/data_warehouse/media_stg.db/zz_creative_daily';


DROP TABLE IF EXISTS `stg`.`ylmf_crowd_daily`;
CREATE TABLE `stg`.`ylmf_crowd_daily` (
  `ad_date` STRING,
  `campaign_group_id` STRING,
  `campaign_group_name` STRING,
  `campaign_id` STRING,
  `campaign_name` STRING,
  `promotion_entity_id` STRING,
  `promotion_entity_name` STRING,
  `sub_crowd_value` STRING,
  `target_name` STRING,
  `target_type` STRING,
  `add_new_charge` STRING,
  `add_new_uv` STRING,
  `add_new_uv_cost` STRING,
  `add_new_uv_rate` STRING,
  `alipay_inshop_amt` STRING,
  `alipay_inshop_num` STRING,
  `avg_access_page_num` STRING,
  `avg_deep_access_times` STRING,
  `cart_num` STRING,
  `charge` STRING,
  `click` STRING,
  `cpc` STRING,
  `cpm` STRING,
  `ctr` STRING,
  `cvr` STRING,
  `deep_inshop_pv` STRING,
  `dir_shop_col_num` STRING,
  `gmv_inshop_amt` STRING,
  `gmv_inshop_num` STRING,
  `icvr` STRING,
  `impression` STRING,
  `inshop_item_col_num` STRING,
  `inshop_potential_uv` STRING,
  `inshop_potential_uv_rate` STRING,
  `inshop_pv` STRING,
  `inshop_pv_rate` STRING,
  `inshop_uv` STRING,
  `prepay_inshop_amt` STRING,
  `prepay_inshop_num` STRING,
  `return_pv` STRING,
  `return_pv_cost` STRING,
  `roi` STRING,
  `search_click_cnt` STRING,
  `search_click_cost` STRING,
  `biz_code` STRING,
  `offset` STRING,
  `page_size` STRING,
  `query_time_dim` STRING,
  `query_domain` STRING,
  `group_by_campaign_id` STRING,
  `group_by_promotion_entity_id` STRING,
  `start_time` STRING,
  `end_time` STRING,
  `effect_type` STRING,
  `effect` STRING,
  `effect_days` STRING,
  `req_storeId` STRING,
  `dw_resource` STRING,
  `dw_create_time` STRING,
  `dw_batch_number` STRING,
  `etl_date` DATE,
  `etl_create_time` TIMESTAMP)
USING delta
LOCATION  'dbfs:/mnt/prod/data_warehouse/media_stg.db/ylmf_crowd_daily';


DROP TABLE IF EXISTS `stg`.`feedflow_crowd_daily`;
CREATE TABLE `stg`.`feedflow_crowd_daily` (
  `store_id` STRING,
  `ad_date` STRING,
  `category_id` STRING,
  `brand_id` STRING,
  `campaign_id` STRING,
  `campaign_name` STRING,
  `adgroup_id` STRING,
  `adgroup_name` STRING,
  `crowd_id` STRING,
  `crowd_name` STRING,
  `effect` DECIMAL(19,4),
  `charge` DECIMAL(19,4),
  `ad_pv` DECIMAL(20,4),
  `clicks` DECIMAL(19,4),
  `ecpm` STRING,
  `ecpc` STRING,
  `inshop_pv` STRING,
  `inshop_uv` STRING,
  `inshop_uv_rate` STRING,
  `deep_inshop_num` DECIMAL(19,4),
  `avg_access_time` STRING,
  `avg_access_page_num` STRING,
  `follow_number` STRING,
  `add_new_uv` STRING,
  `add_new_uv_rate` STRING,
  `new_f_charge` STRING,
  `inshop_item_col_num` STRING,
  `cart_num` DECIMAL(19,4),
  `gmv_inshop_num` DECIMAL(19,4),
  `gmv_inshop_amt` DECIMAL(19,4),
  `alipay_in_shop_num` DECIMAL(19,4),
  `alipay_inshop_amt` DECIMAL(19,4),
  `icvr` STRING,
  `cvr` STRING,
  `roi` STRING,
  `data_source` STRING,
  `dw_etl_date` STRING,
  `dw_batch_id` STRING,
  `dmp` STRING)
USING delta
LOCATION  'dbfs:/mnt/prod/data_warehouse/media_stg.db/feedflow_crowd_daily';


DROP TABLE IF EXISTS `stg`.`super_zz_crowd_daily`;
CREATE TABLE `stg`.`super_zz_crowd_daily` (
  `ad_pv` DECIMAL(20,10),
  `alipay_in_shop_num` DECIMAL(20,10),
  `alipay_inshop_amt` DECIMAL(20,10),
  `alipay_uv` DECIMAL(20,10),
  `alipay_uv_cost` DECIMAL(20,10),
  `avg_access_page_num` DECIMAL(20,10),
  `avg_access_time` DECIMAL(20,10),
  `campaign_group_id` STRING,
  `campaign_group_name` STRING,
  `campaign_id` STRING,
  `campaign_name` STRING,
  `cart_num` DECIMAL(20,10),
  `cost` DECIMAL(20,10),
  `click` DECIMAL(20,10),
  `ctr` DECIMAL(20,10),
  `cvr` DECIMAL(20,10),
  `deep_inshop_uv` DECIMAL(20,10),
  `deep_interest` DECIMAL(20,10),
  `deep_interest_cost` DECIMAL(20,10),
  `dir_shop_col_num` STRING,
  `dw_batch_id` STRING,
  `dw_etl_date` STRING,
  `data_source` STRING,
  `ecpc` DECIMAL(20,10),
  `ecpm` DECIMAL(20,10),
  `gmv_inshop_amt` DECIMAL(20,10),
  `gmv_inshop_num` DECIMAL(20,10),
  `inshop_item_col_num` DECIMAL(20,10),
  `inshop_uv` DECIMAL(20,10),
  `inshop_uv_cost` DECIMAL(20,10),
  `item_shop_cat_cost` DECIMAL(20,10),
  `pv_uv` DECIMAL(20,10),
  `biz_code` STRING,
  `effect` STRING,
  `ad_date` TIMESTAMP,
  `roi` DECIMAL(20,10),
  `shop_id` STRING,
  `category_id` STRING,
  `brand_id` STRING,
  `crowd_id` STRING,
  `crowd_name` STRING,
  `effect_days` STRING,
  `dmp` STRING)
USING delta
LOCATION  'dbfs:/mnt/prod/data_warehouse/media_stg.db/super_zz_crowd_daily';


DROP TABLE IF EXISTS `stg`.`zz_target_daily`;
CREATE TABLE `stg`.`zz_target_daily` (
  `ad_date` TIMESTAMP,
  `store_id` STRING,
  `campaign_id` STRING,
  `campaign_name` STRING,
  `adgroup_id` STRING,
  `adgroup_name` STRING,
  `category_id` STRING,
  `brand_id` STRING,
  `target_audience_id` STRING,
  `target_audience_name` STRING,
  `req_effect_type` STRING,
  `req_campaign_model` STRING,
  `effect` STRING,
  `effect_days` STRING,
  `order_value` DECIMAL(19,4),
  `order_quantity` INT,
  `gmv_value` DECIMAL(19,4),
  `gmv_quantity` INT,
  `total_cart_quantity` INT,
  `favorite_shop_quantity` INT,
  `favorite_item_quantity` INT,
  `avg_access_page_quantity` INT,
  `avg_access_time_length` INT,
  `deep_inshop_uv` INT,
  `uv` INT,
  `cost` DECIMAL(19,4),
  `clicks` INT,
  `impressions` INT,
  `data_source` STRING,
  `dw_etl_date` TIMESTAMP,
  `dw_batch_id` STRING,
  `audience_name` STRING,
  `type` STRING)
USING delta
LOCATION  'dbfs:/mnt/prod/data_warehouse/media_stg.db/zz_target_daily';


DROP TABLE IF EXISTS `stg`.`ylmf_promotion_daily`;
CREATE TABLE `stg`.`ylmf_promotion_daily` (
  `ad_date` STRING,
  `campaign_group_id` STRING,
  `campaign_group_name` STRING,
  `campaign_id` STRING,
  `campaign_name` STRING,
  `promotion_entity_id` STRING,
  `promotion_entity_name` STRING,
  `add_new_charge` STRING,
  `add_new_uv` STRING,
  `add_new_uv_cost` STRING,
  `add_new_uv_rate` STRING,
  `alipay_inshop_amt` STRING,
  `alipay_inshop_num` STRING,
  `avg_access_page_num` STRING,
  `avg_deep_access_times` STRING,
  `cart_num` STRING,
  `charge` STRING,
  `click` STRING,
  `cpc` STRING,
  `cpm` STRING,
  `ctr` STRING,
  `cvr` STRING,
  `deep_inshop_pv` STRING,
  `dir_shop_col_num` STRING,
  `gmv_inshop_amt` STRING,
  `gmv_inshop_num` STRING,
  `icvr` STRING,
  `impression` STRING,
  `inshop_item_col_num` STRING,
  `inshop_potential_uv` STRING,
  `inshop_potential_uv_rate` STRING,
  `inshop_pv` STRING,
  `inshop_pv_rate` STRING,
  `inshop_uv` STRING,
  `prepay_inshop_amt` STRING,
  `prepay_inshop_num` STRING,
  `return_pv` STRING,
  `return_pv_cost` STRING,
  `roi` STRING,
  `search_click_cnt` STRING,
  `search_click_cost` STRING,
  `biz_code` STRING,
  `offset` STRING,
  `page_size` STRING,
  `query_time_dim` STRING,
  `query_domain` STRING,
  `group_by_campaign_id` STRING,
  `group_by_log_date` STRING,
  `start_time` STRING,
  `end_time` STRING,
  `effect_type` STRING,
  `effect` STRING,
  `effect_days` STRING,
  `req_storeId` STRING,
  `dw_resource` STRING,
  `dw_create_time` STRING,
  `dw_batch_number` STRING,
  `etl_date` DATE,
  `etl_create_time` TIMESTAMP)
USING delta
LOCATION  'dbfs:/mnt/prod/data_warehouse/media_stg.db/ylmf_promotion_daily';


DROP TABLE IF EXISTS `stg`.`super_zz_adgroup_daily`;
CREATE TABLE `stg`.`super_zz_adgroup_daily` (
  `ad_pv` DECIMAL(20,4),
  `alipay_in_shop_num` DECIMAL(20,4),
  `alipay_inshop_amt` DECIMAL(20,4),
  `alipay_uv` STRING,
  `alipay_uv_cost` STRING,
  `avg_access_page_num` STRING,
  `avg_access_time` STRING,
  `campaign_group_id` STRING,
  `campaign_group_name` STRING,
  `cart_num` DECIMAL(20,4),
  `cost` DECIMAL(20,4),
  `click` DECIMAL(20,4),
  `ctr` DECIMAL(20,4),
  `cvr` DECIMAL(20,4),
  `deep_inshop_uv` STRING,
  `deep_interest` STRING,
  `deep_interest_cost` STRING,
  `dir_shop_col_num` STRING,
  `dw_batch_id` STRING,
  `dw_etl_date` STRING,
  `data_source` STRING,
  `ecpc` STRING,
  `ecpm` STRING,
  `gmv_inshop_amt` STRING,
  `gmv_inshop_num` DECIMAL(20,4),
  `inshop_item_col_num` STRING,
  `inshop_uv` STRING,
  `inshop_uv_cost` STRING,
  `item_shop_cat_cost` STRING,
  `pv_uv` STRING,
  `biz_code` STRING,
  `effect` STRING,
  `ad_date` TIMESTAMP,
  `roi` DECIMAL(20,4),
  `shop_id` STRING,
  `category_id` STRING,
  `brand_id` STRING,
  `effect_days` STRING)
USING delta
LOCATION  'dbfs:/mnt/prod/data_warehouse/media_stg.db/super_zz_adgroup_daily';


DROP TABLE IF EXISTS `stg`.`feedflow_adgroup_daily`;
CREATE TABLE `stg`.`feedflow_adgroup_daily` (
  `store_id` STRING,
  `ad_date` TIMESTAMP,
  `category_id` STRING,
  `brand_id` STRING,
  `campaign_id` STRING,
  `campaign_name` STRING,
  `adgroup_id` STRING,
  `adgroup_name` STRING,
  `effect` STRING,
  `charge` DECIMAL(20,8),
  `ad_pv` DECIMAL(20,0),
  `click` DECIMAL(20,0),
  `ecpm` DECIMAL(20,10),
  `ecpc` DECIMAL(20,10),
  `inshop_pv` DECIMAL(20,0),
  `inshop_uv` DECIMAL(20,4),
  `inshop_uv_rate` DECIMAL(20,4),
  `deep_inshop_num` DECIMAL(20,4),
  `avg_access_time` DECIMAL(20,10),
  `avg_access_page_num` DECIMAL(20,0),
  `follow_number` DECIMAL(20,0),
  `add_new_uv` DECIMAL(20,0),
  `add_new_uv_rate` DECIMAL(20,10),
  `new_f_charge` DECIMAL(20,8),
  `inshop_item_col_num` DECIMAL(20,0),
  `cart_num` DECIMAL(20,0),
  `gmv_inshop_num` DECIMAL(20,0),
  `gmv_inshop_amt` DECIMAL(20,8),
  `alipay_in_shop_num` DECIMAL(20,0),
  `alipay_inshop_amt` DECIMAL(20,10),
  `icvr` DECIMAL(20,10),
  `cvr` DECIMAL(20,10),
  `roi` DECIMAL(20,10),
  `data_source` STRING,
  `dw_etl_date` STRING,
  `dw_batch_id` STRING)
USING delta
LOCATION  'dbfs:/mnt/prod/data_warehouse/media_stg.db/feedflow_adgroup_daily';


DROP TABLE IF EXISTS `stg`.`zz_adgroup_daily`;
CREATE TABLE `stg`.`zz_adgroup_daily` (
  `ad_date` TIMESTAMP,
  `store_id` STRING,
  `campaign_id` STRING,
  `campaign_name` STRING,
  `adgroup_id` STRING,
  `adgroup_name` STRING,
  `category_id` STRING,
  `brand_id` STRING,
  `req_effect_type` STRING,
  `req_campaign_model` STRING,
  `effect` STRING,
  `effect_days` STRING,
  `order_value` DECIMAL(19,4),
  `order_quantity` DECIMAL(19,4),
  `gmv_value` DECIMAL(19,4),
  `gmv_quantity` INT,
  `total_cart_quantity` INT,
  `favorite_shop_quantity` INT,
  `favorite_item_quantity` INT,
  `avg_access_page_quantity` INT,
  `avg_access_time_length` INT,
  `deep_inshop_uv` INT,
  `uv` INT,
  `cost` DECIMAL(19,4),
  `clicks` BIGINT,
  `impressions` BIGINT,
  `data_source` STRING,
  `dw_etl_date` TIMESTAMP,
  `dw_batch_id` STRING,
  `audience_name` STRING,
  `type` STRING)
USING delta
LOCATION  'dbfs:/mnt/prod/data_warehouse/media_stg.db/zz_adgroup_daily';

DROP TABLE IF EXISTS `stg`.`tmsha_effect_daily`;
CREATE TABLE `stg`.`tmsha_effect_daily` (
  `adzoneName` STRING,
  `brandName` STRING,
  `alipayUv` INT,
  `alipayNum` INT,
  `alipayFee` DOUBLE,
  `req_date` STRING,
  `req_attribution_period` INT,
  `req_group_by_adzone` BOOLEAN,
  `req_group_by_brand` BOOLEAN,
  `req_storeId` INT,
  `dw_resource` STRING,
  `dw_create_time` BIGINT,
  `dw_batch_number` BIGINT,
  `etl_date` DATE,
  `etl_create_time` TIMESTAMP)
USING delta
LOCATION  'dbfs:/mnt/prod/data_warehouse/media_stg.db/tmsha_effect_daily';

DROP TABLE IF EXISTS `stg`.`tmsha_report_daily`;
CREATE TABLE `stg`.`tmsha_report_daily` (
  `adzoneName` STRING,
  `clickUv` INT,
  `clickPv` INT,
  `req_date` STRING,
  `req_group_by_adzone` BOOLEAN,
  `req_storeId` INT,
  `dw_resource` STRING,
  `dw_create_time` BIGINT,
  `dw_batch_number` BIGINT,
  `etl_date` DATE,
  `etl_create_time` TIMESTAMP)
USING delta
LOCATION  'dbfs:/mnt/prod/data_warehouse/media_stg.db/tmsha_report_daily';

DROP TABLE IF EXISTS `stg`.`mxdp_adgroup_daily`;
CREATE TABLE `stg`.`mxdp_adgroup_daily` (
  `adgroupid` BIGINT,
  `adgrouptitle` STRING,
  `campaignid` BIGINT,
  `campaigntitle` STRING,
  `carttotal` INT,
  `click` INT,
  `click_cvr` DOUBLE,
  `click_roi` DOUBLE,
  `click_transactionshipping` INT,
  `click_transactiontotal` DOUBLE,
  `click_uv` INT,
  `cost` DOUBLE,
  `cpc` DOUBLE,
  `cpm` DOUBLE,
  `ctr` DOUBLE,
  `cvr` DOUBLE,
  `favitemtotal` INT,
  `favshoptotal` INT,
  `impression` INT,
  `roi` DOUBLE,
  `thedate` STRING,
  `transactionshippingtotal` INT,
  `transactiontotal` DOUBLE,
  `uv` INT,
  `uv_new` INT,
  `req_traffic_type` STRING,
  `req_start_date` STRING,
  `req_end_date` STRING,
  `req_effect` INT,
  `req_storeId` INT,
  `dw_resource` STRING,
  `dw_create_time` BIGINT,
  `dw_batch_number` BIGINT,
  `req_effect_Days` INT,
  `etl_date` DATE,
  `etl_create_time` TIMESTAMP)
USING delta
LOCATION  'dbfs:/mnt/prod/data_warehouse/media_stg.db/mxdp_adgroup_daily';

DROP TABLE IF EXISTS `stg`.`pptx_adgroup_daily`;
CREATE TABLE `stg`.`pptx_adgroup_daily` (
  `click` INT,
  `click_uv` INT,
  `ctr` DOUBLE,
  `impression` INT,
  `solution_id` INT,
  `solution_name` STRING,
  `target_name` STRING,
  `task_id` INT,
  `task_name` STRING,
  `thedate` STRING,
  `uv` INT,
  `uv_ctr` DOUBLE,
  `req_start_date` STRING,
  `req_end_date` STRING,
  `req_storeId` INT,
  `dw_resource` STRING,
  `dw_create_time` BIGINT,
  `dw_batch_number` BIGINT,
  `etl_date` DATE,
  `etl_create_time` TIMESTAMP)
USING delta
LOCATION  'dbfs:/mnt/prod/data_warehouse/media_stg.db/pptx_adgroup_daily';

DROP TABLE IF EXISTS `stg`.`ppzq_adgroup_daily`;
CREATE TABLE `stg`.`ppzq_adgroup_daily` (
  `adgroupid` BIGINT,
  `adgrouptitle` STRING,
  `campaignid` BIGINT,
  `campaigntitle` STRING,
  `carttotal` INT,
  `click` INT,
  `click_cvr` DOUBLE,
  `click_roi` DOUBLE,
  `click_transactiontotal` DOUBLE,
  `cost` DOUBLE,
  `cpc` DOUBLE,
  `cpm` DOUBLE,
  `ctr` DOUBLE,
  `cvr` DOUBLE,
  `favitemtotal` INT,
  `favshoptotal` INT,
  `impression` INT,
  `roi` DOUBLE,
  `thedate` STRING,
  `transactionshippingtotal` INT,
  `transactiontotal` DOUBLE,
  `uv` INT,
  `uv_new` INT,
  `req_traffic_type` STRING,
  `req_start_date` STRING,
  `req_end_date` STRING,
  `req_effect` INT,
  `req_effect_Days` INT,
  `req_storeId` INT,
  `dw_resource` STRING,
  `dw_create_time` BIGINT,
  `dw_batch_number` BIGINT,
  `etl_date` DATE,
  `etl_create_time` TIMESTAMP)
USING delta
LOCATION  'dbfs:/mnt/prod/data_warehouse/media_stg.db/ppzq_adgroup_daily';
