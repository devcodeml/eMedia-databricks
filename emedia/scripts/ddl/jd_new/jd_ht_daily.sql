DROP TABLE IF EXISTS `dwd`.`tb_media_emedia_jdht_daily_fact`;
CREATE TABLE `dwd`.`tb_media_emedia_jdht_daily_fact` (
  `ad_date` DATE,
  `ad_format_lv2` STRING,
  `pin_name` STRING,
  `effect` STRING,
  `effect_days` STRING,
  `campaign_id` STRING,
  `campaign_name` STRING,
  `report_level` STRING,
  `report_level_id` STRING,
  `report_level_name` STRING,
  `emedia_category_id` STRING,
  `emedia_brand_id` STRING,
  `mdm_category_id` STRING,
  `mdm_brand_id` STRING,
  `mdm_productline_id` STRING,
  `order_status_category` STRING,
  `click_or_order_caliber` STRING,
  `cost` DECIMAL(20,4),
  `click` BIGINT,
  `impression` BIGINT,
  `order_quantity` BIGINT,
  `order_value` DECIMAL(20,4),
  `total_cart_quantity` BIGINT,
  `new_customer_quantity` BIGINT,
  `dw_source` STRING,
  `dw_create_time` STRING,
  `dw_batch_number` STRING,
  `etl_source_table` STRING,
  `etl_update_time` TIMESTAMP,
  `etl_create_time` TIMESTAMP)
USING delta
LOCATION  'dbfs:/mnt/prod/data_warehouse/media_dwd.db/tb_media_emedia_jdht_daily_fact';

DROP TABLE IF EXISTS `dwd`.`jdht_campaign_daily`;
CREATE TABLE `dwd`.`jdht_campaign_daily` (
  `ad_date` STRING,
  `pin_name` STRING,
  `category_id` STRING,
  `brand_id` STRING,
  `campaign_id` STRING,
  `campaign_name` STRING,
  `effect_days` STRING,
  `impressions` STRING,
  `clicks` STRING,
  `cost` STRING,
  `directCartCnt` STRING,
  `directOrderCnt` STRING,
  `directOrderSum` STRING,
  `indirectCartCnt` STRING,
  `indirectOrderCnt` STRING,
  `indirectOrderSum` STRING,
  `totalCartCnt` STRING,
  `totalOrderCnt` STRING,
  `totalOrderSum` STRING,
  `activityType` STRING,
  `commentCnt` STRING,
  `couponCnt` STRING,
  `CPA` STRING,
  `CPC` STRING,
  `CPM` STRING,
  `CPR` STRING,
  `CTR` STRING,
  `depthPassengerCnt` STRING,
  `effectCartCnt` STRING,
  `effectOrderCnt` STRING,
  `effectOrderSum` STRING,
  `followCnt` STRING,
  `goodsAttentionCnt` STRING,
  `interActCnt` STRING,
  `IR` STRING,
  `likeCnt` STRING,
  `newCustomersCnt` STRING,
  `clickDate` STRING,
  `orderStatusCategory` STRING,
  `preorderCnt` STRING,
  `presaleDirectOrderCnt` STRING,
  `presaleDirectOrderSum` STRING,
  `presaleIndirectOrderCnt` STRING,
  `presaleIndirectOrderSum` STRING,
  `shareCnt` STRING,
  `shopAttentionCnt` STRING,
  `totalAuctionCnt` STRING,
  `totalAuctionMarginSum` STRING,
  `totalOrderCVS` STRING,
  `totalOrderROI` STRING,
  `totalPresaleOrderCnt` STRING,
  `totalPresaleOrderSum` STRING,
  `visitorCnt` STRING,
  `visitPageCnt` STRING,
  `visitTimeAverage` STRING,
  `watchCnt` STRING,
  `watchTimeAvg` STRING,
  `watchTimeSum` STRING,
  `req_startDay` STRING,
  `req_endDay` STRING,
  `req_productName` STRING,
  `req_page` STRING,
  `req_pageSize` STRING,
  `req_clickOrOrderDay` STRING,
  `req_clickOrOrderCaliber` STRING,
  `req_isDaily` STRING)
USING delta
LOCATION  'dbfs:/mnt/prod/data_warehouse/media_dwd.db/jdht_campaign_daily';


DROP TABLE IF EXISTS `stg`.`jdht_campaign_daily_old`;
CREATE TABLE `stg`.`jdht_campaign_daily_old` (
  `ad_date` TIMESTAMP,
  `pin_name` STRING,
  `campaign_id` STRING,
  `campaign_name` STRING,
  `status` STRING,
  `category_id` STRING,
  `brand_id` STRING,
  `click_or_order_caliber` DECIMAL(20,4),
  `effect` STRING,
  `impressions` DECIMAL(20,4),
  `clicks` DECIMAL(20,4),
  `cost` DECIMAL(20,4),
  `ctr` DECIMAL(20,4),
  `cpm` DECIMAL(20,4),
  `cpc` DECIMAL(20,4),
  `direct_order_sum` DECIMAL(20,4),
  `indirect_order_cnt` DECIMAL(20,4),
  `indirect_order_sum` DECIMAL(20,4),
  `total_order_cnt` DECIMAL(20,4),
  `total_order_sum` DECIMAL(20,4),
  `direct_cart_cnt` DECIMAL(20,4),
  `indirect_cart_cnt` DECIMAL(20,4),
  `total_cart_cnt` DECIMAL(20,4),
  `total_order_cvs` DECIMAL(20,4),
  `total_order_roi` DECIMAL(20,4),
  `direct_order_cnt` DECIMAL(20,4),
  `department_gmv` DECIMAL(20,0),
  `department_cnt` DECIMAL(20,4),
  `platform_gmv` DECIMAL(20,0),
  `platform_cnt` DECIMAL(20,4),
  `channel_roi` DECIMAL(20,4),
  `new_customers_cnt` DECIMAL(20,4),
  `visitor_cnt` DECIMAL(20,4),
  `visit_page_cnt` DECIMAL(20,4),
  `visit_time_average` DECIMAL(20,4),
  `depth_passenger_cnt` DECIMAL(20,4),
  `shop_attention_cnt` DECIMAL(20,4),
  `goods_attention_cnt` DECIMAL(20,4),
  `preorder_cnt` DECIMAL(20,4),
  `coupon_cnt` DECIMAL(20,4),
  `data_source` STRING,
  `dw_etl_date` STRING,
  `dw_batch_id` STRING)
USING delta
LOCATION  'dbfs:/mnt/prod/data_warehouse/media_stg.db/jdht_campaign_daily_old';

DROP TABLE IF EXISTS `dwd`.`jdht_campaign_daily_old`;
CREATE TABLE `dwd`.`jdht_campaign_daily_old` (
  `ad_date` DATE,
  `pin_name` STRING,
  `campaign_id` STRING,
  `campaign_name` STRING,
  `status` STRING,
  `category_id` STRING,
  `brand_id` STRING,
  `click_or_order_caliber` DECIMAL(20,4),
  `effect` STRING,
  `impressions` DECIMAL(20,4),
  `clicks` DECIMAL(20,4),
  `cost` DECIMAL(20,4),
  `ctr` DECIMAL(20,4),
  `cpm` DECIMAL(20,4),
  `cpc` DECIMAL(20,4),
  `direct_order_sum` DECIMAL(20,4),
  `indirect_order_cnt` DECIMAL(20,4),
  `indirect_order_sum` DECIMAL(20,4),
  `total_order_cnt` DECIMAL(20,4),
  `total_order_sum` DECIMAL(20,4),
  `direct_cart_cnt` DECIMAL(20,4),
  `indirect_cart_cnt` DECIMAL(20,4),
  `total_cart_cnt` DECIMAL(20,4),
  `total_order_cvs` DECIMAL(20,4),
  `total_order_roi` DECIMAL(20,4),
  `direct_order_cnt` DECIMAL(20,4),
  `department_gmv` DECIMAL(20,0),
  `department_cnt` DECIMAL(20,4),
  `platform_gmv` DECIMAL(20,0),
  `platform_cnt` DECIMAL(20,4),
  `channel_roi` DECIMAL(20,4),
  `new_customers_cnt` DECIMAL(20,4),
  `visitor_cnt` DECIMAL(20,4),
  `visit_page_cnt` DECIMAL(20,4),
  `visit_time_average` DECIMAL(20,4),
  `depth_passenger_cnt` DECIMAL(20,4),
  `shop_attention_cnt` DECIMAL(20,4),
  `goods_attention_cnt` DECIMAL(20,4),
  `preorder_cnt` DECIMAL(20,4),
  `coupon_cnt` DECIMAL(20,4),
  `data_source` STRING,
  `dw_etl_date` STRING,
  `dw_batch_id` STRING,
  `mdm_productline_id` STRING,
  `emedia_category_id` STRING,
  `emedia_brand_id` STRING,
  `mdm_category_id` STRING,
  `mdm_brand_id` STRING,
  `effect_days` STRING)
USING delta
LOCATION  'dbfs:/mnt/prod/data_warehouse/media_dwd.db/jdht_campaign_daily_old';
