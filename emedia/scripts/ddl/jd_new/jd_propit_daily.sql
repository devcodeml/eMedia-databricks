DROP TABLE IF EXISTS `stg`.`jd_propit_daily`;
CREATE TABLE `stg`.`jd_propit_daily` (
  `dateTime` STRING,
  `skuId` STRING,
  `skuName` STRING,
  `firstCateId` STRING,
  `firstCateName` STRING,
  `secondCateId` STRING,
  `secondCateName` STRING,
  `thirdCateId` STRING,
  `thirdCateName` STRING,
  `brandId` STRING,
  `brandName` STRING,
  `groupId` STRING,
  `groupName` STRING,
  `activityName` STRING,
  `activityLink` STRING,
  `clickNum` STRING,
  `clickPeople` STRING,
  `dealAmt` STRING,
  `ledDealAmt` STRING,
  `led15DayDealAmt` STRING,
  `cartNum` STRING,
  `ledCartNum` STRING,
  `led15DayCartNum` STRING,
  `dw_batch_num` STRING,
  `dw_create_time` STRING,
  `dw_source_name` STRING,
  `data_source` STRING,
  `dw_batch_id` STRING,
  `dw_etl_date` DATE)
USING delta
LOCATION  'dbfs:/mnt/prod/data_warehouse/media_stg.db/jd_propit_daily';

DROP TABLE IF EXISTS `ods`.`jd_propit_daily`;
CREATE TABLE `ods`.`jd_propit_daily` (
  `date_time` DATE,
  `sku_id` STRING,
  `sku_name` STRING,
  `first_cate_id` STRING,
  `first_cate_name` STRING,
  `second_cate_id` STRING,
  `second_cate_name` STRING,
  `third_cate_id` STRING,
  `third_cate_name` STRING,
  `brand_id` STRING,
  `brand_name` STRING,
  `group_id` STRING,
  `group_name` STRING,
  `activity_name` STRING,
  `activity_link` STRING,
  `click_num` BIGINT,
  `click_people` BIGINT,
  `deal_amt` DECIMAL(19,4),
  `led_deal_amt` DECIMAL(19,4),
  `led15_day_deal_amt` DECIMAL(19,4),
  `cart_num` BIGINT,
  `led_cart_num` BIGINT,
  `led15_day_cart_num` BIGINT,
  `dw_batch_number` STRING,
  `dw_create_time` STRING,
  `dw_source_name` STRING,
  `data_source` STRING,
  `dw_batch_id` STRING,
  `dw_etl_date` DATE)
USING delta
LOCATION  'dbfs:/mnt/prod/data_warehouse/media_ods.db/jd_propit_daily';

DROP TABLE IF EXISTS `dwd`.`tb_media_emedia_jd_propit_daily_fact`;
CREATE TABLE `dwd`.`tb_media_emedia_jd_propit_daily_fact` (
  `date_time` DATE,
  `sku_id` STRING,
  `sku_name` STRING,
  `first_cate_id` STRING,
  `first_cate_name` STRING,
  `second_cate_id` STRING,
  `second_cate_name` STRING,
  `third_cate_id` STRING,
  `third_cate_name` STRING,
  `brand_id` STRING,
  `brand_name` STRING,
  `group_id` STRING,
  `group_name` STRING,
  `activity_name` STRING,
  `activity_link` STRING,
  `click_num` BIGINT,
  `click_people` BIGINT,
  `deal_amt` DECIMAL(19,4),
  `led_deal_amt` DECIMAL(19,4),
  `led15_day_deal_amt` DECIMAL(19,4),
  `cart_num` BIGINT,
  `led_cart_num` BIGINT,
  `led15_day_cart_num` BIGINT,
  `dw_batch_number` STRING,
  `dw_create_time` STRING,
  `dw_source` STRING,
  `etl_source_table` STRING,
  `etl_create_time` TIMESTAMP,
  `etl_update_time` TIMESTAMP)
USING delta
LOCATION  'dbfs:/mnt/prod/data_warehouse/media_dwd.db/tb_media_emedia_jd_propit_daily_fact';

