DROP TABLE IF EXISTS `ods`.`jdzt_account_daily`;
CREATE TABLE `ods`.`jdzt_account_daily` (
  `ad_date` DATE,
  `pin_name` STRING,
  `pin_id` STRING,
  `effect` STRING,
  `effect_days` STRING,
  `cost` DECIMAL(20,4),
  `clicks` BIGINT,
  `impressions` BIGINT,
  `cpa` STRING,
  `cpc` DECIMAL(20,4),
  `cpm` DECIMAL(20,4),
  `ctr` DECIMAL(9,4),
  `total_order_roi` DECIMAL(9,4),
  `total_order_cvs` DECIMAL(9,4),
  `direct_cart_cnt` BIGINT,
  `indirect_cart_cnt` BIGINT,
  `total_cart_quantity` BIGINT,
  `direct_order_value` DECIMAL(20,4),
  `indirect_order_value` DECIMAL(20,4),
  `order_value` DECIMAL(20,4),
  `direct_order_quantity` BIGINT,
  `indirect_order_quantity` BIGINT,
  `order_quantity` BIGINT,
  `favorite_item_quantity` BIGINT,
  `favorite_shop_quantity` BIGINT,
  `coupon_quantity` BIGINT,
  `preorder_quantity` BIGINT,
  `depth_passenger_quantity` BIGINT,
  `new_customer_quantity` BIGINT,
  `visit_page_quantity` BIGINT,
  `visit_time_length` DECIMAL(20,4),
  `visitor_quantity` BIGINT,
  `presale_direct_order_cnt` BIGINT,
  `presale_indirect_order_cnt` BIGINT,
  `total_presale_order_cnt` BIGINT,
  `presale_direct_order_sum` DECIMAL(20,4),
  `presale_indirect_order_sum` DECIMAL(20,4),
  `total_presale_order_sum` DECIMAL(20,4),
  `click_date` DATE,
  `platform_cnt` BIGINT,
  `platform_gmv` DECIMAL(20,4),
  `department_cnt` BIGINT,
  `department_gmv` DECIMAL(20,4),
  `channel_roi` DECIMAL(20,4),
  `mobile_type` STRING,
  `business_type` STRING,
  `gift_flag` STRING,
  `order_status_category` STRING,
  `click_or_order_caliber` STRING,
  `impression_or_click_effect` STRING,
  `start_day` DATE,
  `end_day` DATE,
  `is_daily` STRING,
  `data_source` STRING,
  `dw_batch_id` STRING,
  `dw_etl_date` DATE)
USING delta
LOCATION  'dbfs:/mnt/prod/data_warehouse/media_ods.db/jdzt_account_daily';

DROP TABLE IF EXISTS `ods`.`jdzt_adgroup_daily`;
CREATE TABLE `ods`.`jdzt_adgroup_daily` (
  `ad_date` DATE,
  `pin_name` STRING,
  `effect` STRING,
  `effect_days` STRING,
  `campaign_id` STRING,
  `campaign_name` STRING,
  `adgroup_id` STRING,
  `adgroup_name` STRING,
  `display_scope` STRING,
  `cost` DECIMAL(20,4),
  `clicks` BIGINT,
  `impressions` BIGINT,
  `order_cpa` STRING,
  `cpc` DECIMAL(20,4),
  `cpm` DECIMAL(20,4),
  `ctr` DECIMAL(9,4),
  `total_order_roi` DECIMAL(9,4),
  `total_order_cvs` DECIMAL(9,4),
  `total_cart_quantity` BIGINT,
  `direct_order_value` DECIMAL(20,4),
  `order_value` DECIMAL(20,4),
  `direct_order_quantity` BIGINT,
  `order_quantity` BIGINT,
  `favorite_item_quantity` BIGINT,
  `favorite_shop_quantity` BIGINT,
  `coupon_quantity` BIGINT,
  `preorder_quantity` BIGINT,
  `new_customer_quantity` BIGINT,
  `visitor_quantity` BIGINT,
  `total_presale_order_cnt` BIGINT,
  `total_presale_order_sum` DECIMAL(20,4),
  `ir` DECIMAL(20,4),
  `like_cnt` BIGINT,
  `comment_cnt` BIGINT,
  `follow_cnt` BIGINT,
  `share_cnt` BIGINT,
  `tencent_picture_click_cnt` BIGINT,
  `tencent_comment_cnt` BIGINT,
  `tencent_read_cnt` BIGINT,
  `tencent_like_cnt` BIGINT,
  `tencent_read_ratio` BIGINT,
  `tencent_share_cnt` BIGINT,
  `tencent_follow_cnt` BIGINT,
  `interact_cnt` BIGINT,
  `live_cost` DECIMAL(20,4),
  `form_commit_cnt` BIGINT,
  `form_commit_cost` DECIMAL(20,4),
  `order_uv` BIGINT,
  `landing_page_uv` BIGINT,
  `market_type` STRING,
  `pull_customer_cpc` DECIMAL(20,4),
  `new_order_uv` BIGINT,
  `cp_visit` DECIMAL(20,4),
  `delivery_target` STRING,
  `video_trend_valid` STRING,
  `cart_cpa` DECIMAL(20,4),
  `control_type` STRING,
  `site_id` STRING,
  `pull_customer_cnt` BIGINT,
  `click_date` DATE,
  `media_type` STRING,
  `business_type` STRING,
  `gift_flag` STRING,
  `order_status_category` STRING,
  `click_or_order_caliber` STRING,
  `impression_or_click_effect` STRING,
  `start_day` DATE,
  `end_day` DATE,
  `is_daily` STRING,
  `data_source` STRING,
  `dw_batch_id` STRING,
  `dw_etl_date` DATE)
USING delta
LOCATION  'dbfs:/mnt/prod/data_warehouse/media_ods.db/jdzt_adgroup_daily';

DROP TABLE IF EXISTS `ods`.`jdzw_campaign_daily`;
CREATE TABLE `ods`.`jdzw_campaign_daily` (
  `ad_date` DATE,
  `pin_name` STRING,
  `effect` STRING,
  `effect_days` STRING,
  `campaign_id` STRING,
  `campaign_name` STRING,
  `cost` DECIMAL(20,4),
  `clicks` BIGINT,
  `impressions` BIGINT,
  `cpa` STRING,
  `cpc` DECIMAL(20,4),
  `cpm` DECIMAL(20,4),
  `ctr` DECIMAL(9,4),
  `total_order_roi` DECIMAL(9,4),
  `total_order_cvs` DECIMAL(9,4),
  `direct_cart_cnt` BIGINT,
  `indirect_cart_cnt` BIGINT,
  `total_cart_quantity` BIGINT,
  `direct_order_value` DECIMAL(20,4),
  `indirect_order_value` DECIMAL(20,4),
  `order_value` DECIMAL(20,4),
  `direct_order_quantity` BIGINT,
  `indirect_order_quantity` BIGINT,
  `order_quantity` BIGINT,
  `favorite_item_quantity` BIGINT,
  `favorite_shop_quantity` BIGINT,
  `coupon_quantity` BIGINT,
  `preorder_quantity` BIGINT,
  `depth_passenger_quantity` BIGINT,
  `new_customer_quantity` BIGINT,
  `visit_page_quantity` BIGINT,
  `visit_time_length` DECIMAL(20,4),
  `visitor_quantity` BIGINT,
  `presale_direct_order_cnt` BIGINT,
  `presale_direct_order_sum` DECIMAL(20,4),
  `presale_indirect_order_cnt` BIGINT,
  `presale_indirect_order_sum` DECIMAL(20,4),
  `total_presale_order_cnt` BIGINT,
  `total_presale_order_sum` DECIMAL(20,4),
  `put_type` STRING,
  `campaign_type` STRING,
  `campaign_put_type` STRING,
  `mobile_type` STRING,
  `business_type` STRING,
  `gift_flag` STRING,
  `order_status_category` STRING,
  `click_or_order_caliber` STRING,
  `impression_or_click_effect` STRING,
  `click_date` DATE,
  `delivery_version` STRING,
  `start_day` DATE,
  `end_day` DATE,
  `is_daily` STRING,
  `data_source` STRING,
  `dw_batch_id` STRING,
  `dw_etl_date` DATE)
USING delta
LOCATION  'dbfs:/mnt/prod/data_warehouse/media_ods.db/jdzw_campaign_daily';

DROP TABLE IF EXISTS `ods`.`jdzw_creative_daily`;
CREATE TABLE `ods`.`jdzw_creative_daily` (
  `ad_date` DATE,
  `pin_name` STRING,
  `effect` STRING,
  `effect_days` STRING,
  `campaign_id` STRING,
  `campaign_name` STRING,
  `adgroup_id` STRING,
  `adgroup_name` STRING,
  `ad_id` STRING,
  `ad_name` STRING,
  `cost` DECIMAL(20,4),
  `clicks` BIGINT,
  `impressions` BIGINT,
  `cpa` STRING,
  `cpc` DECIMAL(20,4),
  `cpm` DECIMAL(20,4),
  `ctr` DECIMAL(9,4),
  `total_order_roi` DECIMAL(9,4),
  `total_order_cvs` DECIMAL(9,4),
  `direct_cart_cnt` BIGINT,
  `indirect_cart_cnt` BIGINT,
  `total_cart_quantity` BIGINT,
  `direct_order_value` DECIMAL(20,4),
  `indirect_order_value` DECIMAL(20,4),
  `order_value` DECIMAL(20,4),
  `direct_order_quantity` BIGINT,
  `indirect_order_quantity` BIGINT,
  `order_quantity` BIGINT,
  `favorite_item_quantity` BIGINT,
  `favorite_shop_quantity` BIGINT,
  `coupon_quantity` BIGINT,
  `preorder_quantity` BIGINT,
  `depth_passenger_quantity` BIGINT,
  `new_customer_quantity` BIGINT,
  `visit_time_length` DECIMAL(20,4),
  `visitor_quantity` BIGINT,
  `visit_page_quantity` BIGINT,
  `presale_direct_order_cnt` BIGINT,
  `presale_indirect_order_cnt` BIGINT,
  `total_presale_order_cnt` BIGINT,
  `presale_direct_order_sum` DECIMAL(20,4),
  `presale_indirect_order_sum` DECIMAL(20,4),
  `total_presale_order_sum` DECIMAL(20,4),
  `follow_cnt` STRING,
  `like_cnt` STRING,
  `interact_cnt` STRING,
  `comment_cnt` STRING,
  `pull_customer_cnt` STRING,
  `pull_customer_cpc` STRING,
  `share_cnt` STRING,
  `ir` DECIMAL(20,4),
  `click_date` DATE,
  `status` STRING,
  `campaign_type` STRING,
  `mobile_type` STRING,
  `business_type` STRING,
  `gift_flag` STRING,
  `order_status_category` STRING,
  `click_or_order_caliber` STRING,
  `impression_or_click_effect` STRING,
  `start_day` DATE,
  `end_day` DATE,
  `is_daily` STRING,
  `data_source` STRING,
  `dw_batch_id` STRING,
  `dw_etl_date` DATE)
USING delta
LOCATION  'dbfs:/mnt/prod/data_warehouse/media_ods.db/jdzw_creative_daily';

DROP TABLE IF EXISTS `ods`.`jdfa_order_daily`;
CREATE TABLE `ods`.`jdfa_order_daily` (
  `order_type` STRING,
  `cost` DECIMAL(19,4),
  `order_no` STRING,
  `pin_name` STRING,
  `ad_date` DATE,
  `campaign_id` STRING,
  `user_id` STRING,
  `ordertype7` STRING,
  `total_amount` DECIMAL(19,4),
  `req_beginDate` STRING,
  `req_endDate` STRING,
  `money_type` STRING,
  `req_subPin` STRING,
  `pin` STRING,
  `dw_create_time` STRING,
  `dw_source` STRING,
  `dw_batch_number` STRING,
  `etl_date` DATE,
  `etl_create_time` TIMESTAMP)
USING delta
LOCATION  'dbfs:/mnt/prod/data_warehouse/media_ods.db/jdfa_order_daily';

DROP TABLE IF EXISTS `ods`.`jd_ppzq_campaign_daily`;
CREATE TABLE `ods`.`jd_ppzq_campaign_daily` (
  `brand_name` STRING,
  `campaign_name` STRING,
  `clicks` BIGINT,
  `ctr` DOUBLE,
  `ad_date` DATE,
  `firstCateName` STRING,
  `impressions` BIGINT,
  `realPrice` DOUBLE,
  `space_name` STRING,
  `uv_impressions` BIGINT,
  `secondCateName` STRING,
  `total_cart_cnt` BIGINT,
  `total_deal_order_cnt` BIGINT,
  `total_deal_order_sum` DECIMAL(19,4),
  `pin_name` STRING,
  `etl_date` DATE,
  `etl_create_time` TIMESTAMP)
USING delta
LOCATION  'dbfs:/mnt/prod/data_warehouse/media_ods.db/jd_ppzq_campaign_daily';









DROP TABLE IF EXISTS `ods`.`jdkc_target_daily`;
CREATE TABLE `ods`.`jdkc_target_daily` (
  `ad_date` DATE,
  `pin_name` STRING,
  `effect` STRING,
  `effect_days` STRING,
  `campaign_id` STRING,
  `campaign_name` STRING,
  `adgroup_id` STRING,
  `adgroup_name` STRING,
  `target_audience_id` STRING,
  `target_audience_name` STRING,
  `dmpFactor` BIGINT,
  `dmpStatus` BOOLEAN,
  `cost` DECIMAL(20,4),
  `clicks` BIGINT,
  `impressions` BIGINT,
  `cpc` DECIMAL(20,4),
  `cpm` DECIMAL(20,4),
  `ctr` DECIMAL(9,4),
  `total_order_roi` DECIMAL(9,4),
  `total_order_cvs` DECIMAL(9,4),
  `direct_cart_cnt` BIGINT,
  `indirect_cart_cnt` BIGINT,
  `total_cart_quantity` BIGINT,
  `direct_order_value` DECIMAL(20,4),
  `indirect_order_value` DECIMAL(20,4),
  `order_value` DECIMAL(20,4),
  `direct_order_quantity` BIGINT,
  `indirect_order_quantity` BIGINT,
  `order_quantity` BIGINT,
  `gift_flag` STRING,
  `order_status_category` STRING,
  `click_or_order_caliber` STRING,
  `start_day` DATE,
  `end_day` DATE,
  `dw_source` STRING,
  `etl_source_table` STRING,
  `dw_batch_id` STRING,
  `req_page` INT,
  `req_pageSize` INT,
  `dw_create_time` BIGINT,
  `dw_etl_date` DATE)
USING delta
LOCATION  'dbfs:/mnt/prod/data_warehouse/media_ods.db/jdkc_target_daily';
